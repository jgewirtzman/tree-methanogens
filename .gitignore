# ==============================================================================
# .gitignore — tree-methanogens
# ==============================================================================
# Strategy: Track code, figures, and directory structure.
# Data file *contents* are archived on Zenodo; only .gitkeep files are tracked
# to preserve the directory layout for reproducibility.
# ==============================================================================

# --- R project files ---
.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj

# --- macOS ---
.DS_Store

# --- Data files (archived on Zenodo, not in git) ---
# Raw data
data/raw/**/*.csv
data/raw/**/*.txt
data/raw/**/*.tsv
data/raw/**/*.xlsx
data/raw/**/*.xls
data/raw/**/*.RData
data/raw/**/*.rds
data/raw/**/*.biom
data/raw/**/*.fasta
data/raw/**/*.fastq
data/raw/**/*.gz
data/raw/**/*.nwk
data/raw/**/*.gsheet
data/raw/**/*.mxd
data/raw/**/*.docx
data/raw/**/*.png
data/raw/**/*.json

# Processed data (regenerable from code + raw data)
data/processed/**/*.csv
# Exception: curated definitions files (canonical inputs, not generated)
!data/processed/molecular/methanotroph_definitions.csv
data/processed/**/*.txt
data/processed/**/*.tsv
data/processed/**/*.xlsx
data/processed/**/*.RData
data/processed/**/*.rds

# Raw data subdirectories (nested directory structures)
data/raw/lgr/semirigid_2020-2021/
data/raw/lgr/static_2021/
data/raw/lgr/static_2023/
data/raw/inventory/spatial_data/
data/raw/inventory/jon_r_files/
data/raw/internal_gas/picarro/
data/raw/ddpcr/Data Files/
data/raw/field_data/static_chamber_scratch/

# Processed metadata
data/processed/metadata/

# Keep .gitkeep files to preserve directory structure
!data/**/.gitkeep

# --- Model objects (large binary files) ---
outputs/models/*.RData
outputs/models/*.rds

# --- Deprecated directory (old code/data, not tracked) ---
deprecated/

# --- Large diagnostic outputs ---
outputs/figures/LGR3_flux_plots_*.pdf

# --- Stray output files ---
outputs/output.txt
outputs/auxfile_goFlux_soilflux_with_weather.txt

# --- Temporary/auto-generated files ---
Rplots.pdf
*.Rplots.pdf
